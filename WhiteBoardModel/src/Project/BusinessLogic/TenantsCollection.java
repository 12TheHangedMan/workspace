package Project.BusinessLogic;

public class TenantsCollection {
    //suppose to use database + collection to store all tenants information, here will use an array instead.
    public static Tenant[] tenants = new Tenant[50];
}
